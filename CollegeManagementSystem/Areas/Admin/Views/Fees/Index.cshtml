@{
    ViewData["Title"] = "Fee";
}

<div>

    <div class="row mb-4">
        <div class="col-6">
            <h2 class="m-0">Fees</h2>
        </div>

        <div class="col-6">
            <a asp-area="Admin" asp-controller="Fees" asp-action="Create" class="btn btn-primary">Add Session</a>
        </div>
    </div>

    <div class="row">

        <div class="col-12">

            <table class="table table-bordered" id="FeesTable">

                <thead>
                    <tr>
                        <th>Student</th>
                        <th>Amount</th>
                        <th>Due Date</th>
                        <th>Paid Date</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
            </table>

        </div>

    </div>

</div>


@section scripts {

    <script>

        let FeesTable;

        FeesTable = $('#FeesTable').DataTable({

            "ajax": {
                "url": "/Admin/Fees/GetFees"
            },
            "columns": [

                {
                    "data": "studentRegistration.applicationUser",
                    "render": function (data) {
                        return `${data.firstName} ${data.lastName}`;
                    }
                },
                { "data": "amount" },
                {
                    "data": "dueDate",
                    "render": function (data) {
                        const date = new Date(data);
                        return date.toLocaleDateString('en-PK');
                    }
                },
                {
                    "data": "paidDate",
                    "render": function (data) {
                        const date = new Date(data);
                        return date.toLocaleDateString('en-PK');
                    }
                },
                { "data": "status" },
                {
                    "data": "id",
                    "render": function (data) {
                        return `
                                    <a class="btn btn-primary" href="/Admin/Fees/Update?id=${data}">Edit</a>
                                    <a class="btn btn-danger" onclick=RemoveFees("/Admin/Fees/Delete/${data}")>Delete</a>
                                `
                    }
                }

            ]


        });


        function RemoveFees(url) {

            Swal.fire({
                title: "Are you sure?",
                text: "You won't be able to revert this!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#3085d6",
                cancelButtonColor: "#d33",
                confirmButtonText: "Yes, delete it!"
            }).then((result) => {
                if (result.isConfirmed) {

                    var xmlHttp = new XMLHttpRequest();


                    xmlHttp.onreadystatechange = () => {

                        if (xmlHttp.readyState == 4 && (xmlHttp.status >= 200 && xmlHttp.status < 400)) {

                            var result = JSON.parse(xmlHttp.responseText);

                            if (result.result) {
                                FeesTable.ajax.reload();
                                toastr.success(result.message);
                            }
                            else {
                                toastr.error(message);
                            }

                        }

                    }
                    xmlHttp.open('DELETE', url, true);
                    xmlHttp.send();

                }




            });


        }

    </script>

}